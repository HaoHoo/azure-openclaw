name: azure-openclaw
metadata:
  template: ./infra/main.bicep
services:
  openclaw-vm:
    project: ./infra
    host: vm
    language: sh
hooks:
  # 部署完成后，利用 az vm run-command 进行最后的配置注入，无需开启 SSH
  postprovision:
    shell: sh
    path: ./scripts/postconfig.sh